<ul class="nav navbar-nav navbar-right">
  {{#if currentUser}}

    <li class="dropdown">
      <a class="dropdown-toggle" data-toggle="dropdown" href="#" id="download" aria-expanded="true">{{req.user.displayName}} <span class="caret"></span></a>
      <ul class="dropdown-menu" aria-labelledby="download">
{{!--         <li>
          {{#link-to 'user.findOne' req.user.id}}{{t 'Profile'}}{{/link-to}}
        </li> --}}
        <li><a href="/conference/1/register">
          <i class="glyphicon glyphicon-plus"></i> {{t 'conference.btn.registration'}}
        </a></li>

        <li class="divider"></li>
        <li>{{#link-to 'auth.logout'}}
          <i class="glyphicon glyphicon-log-out"></i> {{t 'Logout'}}
        {{/link-to}}</li>
      </ul>
    </li>

  {{else}}
    <li>{{#link-to 'auth.loginPage'}}{{t "login.block.button.text"}}{{/link-to}}</li>
{{!--     <li>{{#link-to 'auth.signupPage'}}{{t "login.block.form.new.account.link"}}{{/link-to}}</li> --}}
  {{/if}}
</ul>